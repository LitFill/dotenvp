module interface main
 
//------------------------------
//#kki: import declarations
 
import std/core/types = std/core/types pub = "";
import std/core/hnd = std/core/hnd pub = "";
import std/core/exn = std/core/exn pub = "";
import std/core/bool = std/core/bool pub = "";
import std/core/order = std/core/order pub = "";
import std/core/char = std/core/char pub = "";
import std/core/int = std/core/int pub = "";
import std/core/vector = std/core/vector pub = "";
import std/core/string = std/core/string pub = "";
import std/core/sslice = std/core/sslice pub = "";
import std/core/list = std/core/list pub = "";
import std/core/maybe = std/core/maybe pub = "";
import std/core/maybe2 = std/core/maybe2 pub = "";
import std/core/either = std/core/either pub = "";
import std/core/tuple = std/core/tuple pub = "";
import std/core/lazy = std/core/lazy pub = "";
import std/core/show = std/core/show pub = "";
import std/core/debug = std/core/debug pub = "";
import std/core/delayed = std/core/delayed pub = "";
import std/core/console = std/core/console pub = "";
import std/core = std/core = "";
 
//------------------------------
//#kki: external imports
 
 
//------------------------------
//#kki: fixity declarations
 
 
//------------------------------
//#kki: local imported aliases
 
 
//------------------------------
//#kki: type declarations
 
pub value{1,0,1} type annotation[85,1,86,8] {
  pub con Export[86,3,86,8]{0,0,0} : annotation = 1;
};
pub value{0,1,8} type name[55,1,55,30] {
  pub con Name[55,12,55,15](name: std/core/types/string){0,1,8} : (name : std/core/types/string) -> name = 1;
};
pub value{0,1,8} type value[71,1,72,22] {
  pub con Str[72,3,72,5](string: std/core/types/string){0,1,8} : (string : std/core/types/string) -> value = 1;
};
pub type item[31,1,33,32] {
  pub con Pair[32,3,32,6](name: name, value: value, annotation: (std/core/types/list :: V -> V)<annotation>, inline-comment: std/core/types/string){0,4,8} : (name : name, value : value, annotation : (std/core/types/list :: V -> V)<annotation>, inline-comment : std/core/types/string) -> item = 1;
  pub con Line-comment[33,3,33,14](content: std/core/types/string){0,1,8} : (content : std/core/types/string) -> item = 2;
};
// AST
pub value{0,1,8} type document[11,1,12,20] {
  // AST
  pub con Document[11,12,11,19](items: (std/core/types/list :: V -> V)<item>){0,1,8} : (items : (std/core/types/list :: V -> V)<item>) -> document = 1;
};
 
//------------------------------
//#kki: declarations
 
// Automatically generated. Retrieves the `items` constructor field of the `:document` type.
pub fun document/items[12,3,12,7] : (^ document : document) -> (std/core/types/list :: V -> V)<item>;
pub fun document/@copy[11,12,11,19] : (@this : document, items : (std/core/types/@optional :: V -> V)<(std/core/types/list :: V -> V)<item>>) -> document;
// Automatically generated. Retrieves the `name` constructor field of the `:name` type.
pub fun name/name[55,17,55,20] : (^ name : name) -> std/core/types/string;
pub fun annotation/eq[92,9,92,21] : (an__derived_eq_1 : annotation, an__derived_eq_2 : annotation) -> std/core/types/bool;
pub fun annotation/(==)[96,9,96,23] : (x : annotation, y : annotation) -> std/core/types/bool;
pub fun name/eq[65,9,65,15] : (na__derived_eq_1 : name, na__derived_eq_2 : name) -> std/core/types/bool;
pub fun name/(==)[68,9,68,17] : (x : name, y : name) -> std/core/types/bool;
pub fun value/eq[78,9,78,16] : (va__derived_eq_1 : value, va__derived_eq_2 : value) -> std/core/types/bool;
pub fun value/(==)[82,9,82,18] : (x : value, y : value) -> std/core/types/bool;
pub fun item/eq[46,9,46,15] : (it__derived_eq_1 : item, it__derived_eq_2 : item) -> std/core/types/bool;
pub fun item/(==)[52,9,52,17] : (x : item, y : item) -> std/core/types/bool;
pub fun document/eq[19,9,19,19] : (do__derived_eq_1 : document, do__derived_eq_2 : document) -> std/core/types/bool;
pub fun document/(==)[22,9,22,21] : (x : document, y : document) -> std/core/types/bool;
pub fun document/to-env[25,9,25,23] : (d : document) -> (std/core/types/list :: V -> V)<(name, value)>;
// Automatically generated. Tests for the `Pair` constructor of the `:item` type.
pub fip fun is-pair[32,3,32,6] : (^ item : item) -> std/core/types/bool;
// Automatically generated. Tests for the `Line-comment` constructor of the `:item` type.
pub fip fun is-line-comment[33,3,33,14] : (^ item : item) -> std/core/types/bool;
pub fun name/@copy[55,12,55,15] : (@this : name, name : (std/core/types/@optional :: V -> V)<std/core/types/string>) -> name;
pub fun name/make[57,9,57,17] : (s : std/core/types/string) -> name;
// Automatically generated. Retrieves the `string` constructor field of the `:value` type.
pub fun value/string[72,7,72,12] : (^ value : value) -> std/core/types/string;
pub fun value/@copy[72,3,72,5] : (@this : value, string : (std/core/types/@optional :: V -> V)<std/core/types/string>) -> value;
pub fun annotation/@copy[86,3,86,8] : (@this : annotation) -> annotation;
pub fun annotation/show[88,9,88,23] : (an__derived_show : annotation) -> std/core/types/string;
pub fun name/show[60,9,60,17] : (na__derived_show : name) -> std/core/types/string;
pub fun value/show[74,9,74,18] : (va__derived_show : value) -> std/core/types/string;
pub fun item/show[35,9,35,17] : (it__derived_show : item) -> std/core/types/string;
pub fun document/show[14,9,14,21] : (do__derived_show : document) -> std/core/types/string;
fun to-pattern[99,12,99,21] : (s : std/core/types/string) -> std/core/types/string;
pub fun eval[101,9,101,12] : (doc : document, value : value) -> value;
// monadic lift
fun @mlift-fixpoint@10055[1,0,1,0] : forall<a,(e :: E)> (?(==) : (a, a) -> std/core/types/bool, current : a, f : (a) -> <(std/core/types/div :: X)|(e :: E)> a, next : a) -> <(std/core/types/div :: X)|(e :: E)> a;
fun fixpoint[108,5,108,12] : forall<a,(e :: E)> (current : a, f : (a) -> <(std/core/types/div :: X)|(e :: E)> a, ?(==) : (a, a) -> std/core/types/bool) -> <(std/core/types/div :: X)|(e :: E)> a;
pub fun document/interp[113,9,113,23] : (doc : document) -> document;
val example/document[125,5,125,20] : document;
fun main[132,5,132,8] : () -> <(std/core/types/div :: X)> document;
 
//------------------------------
//#kki: external declarations
 
 
//------------------------------
//#kki: inline definitions
 
//.inline-section
recursive specialize "_**" fun fixpoint // inline size: 5
  = forall<a,(e :: E)> fn<<(std/core/types/div :: X)|(e :: E)>>(current: a, f: (a) -> <(std/core/types/div :: X)|(e :: E)> a, @implicit/(==): (a, a) -> std/core/types/bool){
    val next[109,7,109,10] : a
          = f(current);
    (match ((std/core/types/@open<(std/core/types/total :: E),<(std/core/types/div :: X)|(e :: E)>,(a, a) -> std/core/types/bool,(a, a) -> <(std/core/types/div :: X)|(e :: E)> std/core/types/bool>(@implicit/(==))(next, current))) {
      ((std/core/types/True() : std/core/types/bool ) as @pat: std/core/types/bool)
         -> next;
      ((@skip std/core/types/False() : std/core/types/bool ) as @pat@0: std/core/types/bool)
         -> main/fixpoint<a,(e :: E)>(next, f, @implicit/(==));
    });
  };
specialize "*" fun document/interp // inline size: 1
  = fn(doc: document){
    val res[114,7,114,9] : (std/core/types/list :: V -> V)<item>
          = std/core/list/map<item,item,(std/core/types/total :: E)>((match (doc) {
              ((@skip main/Document((@x: (std/core/types/list :: V -> V)<item>) : (std/core/types/list :: V -> V)<item>) : document ) as @pat: document)
                 -> @x;
            }), (fn(i: item){
              (match (i) {
                ((main/Pair(((@skip main/Name((@pat@0@0: std/core/types/string) : std/core/types/string) : name ) as n: name) : name, ((@skip main/Str((@pat@1: std/core/types/string) : std/core/types/string) : value ) as v: value) : value, (a: (std/core/types/list :: V -> V)<annotation>) : (std/core/types/list :: V -> V)<annotation>, (c: std/core/types/string) : std/core/types/string) : item ) as @pat@0: item)
                   -> main/Pair(n, (main/eval(doc, v)), a, c);
                ((@skip main/Line-comment((c@0: std/core/types/string) : std/core/types/string) : item ) as @pat@2: item)
                   -> main/Line-comment(c@0);
              });
            }));
    main/Document(res);
  };
inline borrow "^" fun document/items // inline size: 0
  = fn(document: document){
    match (document) {
      ((@skip main/Document((@x: (std/core/types/list :: V -> V)<item>) : (std/core/types/list :: V -> V)<item>) : document ) as @pat: document)
         -> @x;
    };
  };
fun document/@copy // inline size: 2
  = fn(@this: document, items: (std/core/types/@optional :: V -> V)<(std/core/types/list :: V -> V)<item>>){
    main/Document((match (items) {
      ((std/core/types/@Optional((@uniq-items@127: (std/core/types/list :: V -> V)<item>) : (std/core/types/list :: V -> V)<item>) : (std/core/types/@optional :: V -> V)<(std/core/types/list :: V -> V)<item>> ) as @pat: ((std/core/types/@optional :: V -> V)<(std/core/types/list :: V -> V)<item>>))
         -> @uniq-items@127;
      ((@skip std/core/types/@None() : (std/core/types/@optional :: V -> V)<(std/core/types/list :: V -> V)<item>> ) as @pat@0: ((std/core/types/@optional :: V -> V)<(std/core/types/list :: V -> V)<item>>))
         -> (match (@this) {
          ((@skip main/Document((@x: (std/core/types/list :: V -> V)<item>) : (std/core/types/list :: V -> V)<item>) : document ) as @pat@1: document)
             -> @x;
        });
    }));
  };
inline borrow "^" fun name/name // inline size: 0
  = fn(name: name){
    match (name) {
      ((@skip main/Name((@x: std/core/types/string) : std/core/types/string) : name ) as @pat: name)
         -> @x;
    };
  };
inline fun annotation/eq // inline size: 0
  = fn(an__derived_eq_1: annotation, an__derived_eq_2: annotation){
    match (an__derived_eq_1, an__derived_eq_2) {
      ((@skip main/Export() : annotation ) as @pat@1: annotation), ((@skip main/Export() : annotation ) as @pat@0: annotation)
         -> std/core/types/True;
    };
  };
inline fun annotation/(==) // inline size: 0
  = fn(x: annotation, y: annotation){
    match (x, y) {
      ((@skip main/Export() : annotation ) as @pat@1: annotation), ((@skip main/Export() : annotation ) as @pat@0: annotation)
         -> std/core/types/True;
    };
  };
fun name/eq // inline size: 1
  = fn(na__derived_eq_1: name, na__derived_eq_2: name){
    std/core/string/(==)((match (na__derived_eq_1) {
        ((@skip main/Name((@x: std/core/types/string) : std/core/types/string) : name ) as @pat: name)
           -> @x;
      }), (match (na__derived_eq_2) {
        ((@skip main/Name((@x@0: std/core/types/string) : std/core/types/string) : name ) as @pat@0: name)
           -> @x@0;
      }));
  };
fun name/(==) // inline size: 1
  = fn(x: name, y: name){
    std/core/string/(==)((match (x) {
        ((@skip main/Name((@x: std/core/types/string) : std/core/types/string) : name ) as @pat: name)
           -> @x;
      }), (match (y) {
        ((@skip main/Name((@x@0: std/core/types/string) : std/core/types/string) : name ) as @pat@0: name)
           -> @x@0;
      }));
  };
fun value/eq // inline size: 1
  = fn(va__derived_eq_1: value, va__derived_eq_2: value){
    match (va__derived_eq_1, va__derived_eq_2) {
      ((@skip main/Str((va__derived_eq_21: std/core/types/string) : std/core/types/string) : value ) as @pat@1: value), ((@skip main/Str((va__derived_eq_11: std/core/types/string) : std/core/types/string) : value ) as @pat@0: value)
         -> std/core/string/(==)(va__derived_eq_11, va__derived_eq_21);
    };
  };
fun value/(==) // inline size: 1
  = fn(x: value, y: value){
    match (x, y) {
      ((@skip main/Str((va__derived_eq_21: std/core/types/string) : std/core/types/string) : value ) as @pat@1: value), ((@skip main/Str((va__derived_eq_11: std/core/types/string) : std/core/types/string) : value ) as @pat@0: value)
         -> std/core/string/(==)(va__derived_eq_11, va__derived_eq_21);
    };
  };
fun item/(==) // inline size: 1
  = fn(x: item, y: item){
    main/#item/eq(x, y);
  };
fun document/eq // inline size: 1
  = fn(do__derived_eq_1: document, do__derived_eq_2: document){
    std/core/list/(==)<item,(std/core/types/total :: E)>((match (do__derived_eq_1) {
        ((@skip main/Document((@x: (std/core/types/list :: V -> V)<item>) : (std/core/types/list :: V -> V)<item>) : document ) as @pat: document)
           -> @x;
      }), (match (do__derived_eq_2) {
        ((@skip main/Document((@x@0: (std/core/types/list :: V -> V)<item>) : (std/core/types/list :: V -> V)<item>) : document ) as @pat@0: document)
           -> @x@0;
      }), main/#item/(==));
  };
fun document/(==) // inline size: 1
  = fn(x: document, y: document){
    std/core/list/(==)<item,(std/core/types/total :: E)>((match (x) {
        ((@skip main/Document((@x: (std/core/types/list :: V -> V)<item>) : (std/core/types/list :: V -> V)<item>) : document ) as @pat: document)
           -> @x;
      }), (match (y) {
        ((@skip main/Document((@x@0: (std/core/types/list :: V -> V)<item>) : (std/core/types/list :: V -> V)<item>) : document ) as @pat@0: document)
           -> @x@0;
      }), main/#item/(==));
  };
fun document/to-env // inline size: 4
  = fn(d: document){
    std/core/list/flatmap<item,(name, value),(std/core/types/total :: E)>((match (d) {
        ((@skip main/Document((@x: (std/core/types/list :: V -> V)<item>) : (std/core/types/list :: V -> V)<item>) : document ) as @pat: document)
           -> @x;
      }), (fn(i: item){
        (match (i) {
          ((main/Pair(((@skip main/Name((@pat@0@0: std/core/types/string) : std/core/types/string) : name ) as n: name) : name, ((@skip main/Str((@pat@1: std/core/types/string) : std/core/types/string) : value ) as v: value) : value, (@pat@2: (std/core/types/list :: V -> V)<annotation>) : (std/core/types/list :: V -> V)<annotation>, (@pat@3: std/core/types/string) : std/core/types/string) : item ) as @pat@0: item)
             -> std/core/types/Cons<(name, value)>((std/core/types/Tuple2<name,value>(n, v)), (std/core/types/Nil<(name, value)>));
          ((@skip main/Line-comment((@pat@5: std/core/types/string) : std/core/types/string) : item ) as @pat@4: item)
             -> std/core/types/Nil<(name, value)>;
        });
      }));
  };
inline borrow "^" fip fun is-pair // inline size: 1
  = fn(item: item){
    match (item) {
      ((main/Pair(((@skip main/Name((@pat@1: std/core/types/string) : std/core/types/string) : name ) as @pat@0: name) : name, ((@skip main/Str((@pat@3: std/core/types/string) : std/core/types/string) : value ) as @pat@2: value) : value, (@pat@4: (std/core/types/list :: V -> V)<annotation>) : (std/core/types/list :: V -> V)<annotation>, (@pat@5: std/core/types/string) : std/core/types/string) : item ) as @pat: item)
         -> std/core/types/True;
      ((@skip main/Line-comment((@pat@7: std/core/types/string) : std/core/types/string) : item ) as @pat@6: item)
         -> std/core/types/False;
    };
  };
inline borrow "^" fip fun is-line-comment // inline size: 1
  = fn(item: item){
    match (item) {
      ((main/Line-comment((@pat@0: std/core/types/string) : std/core/types/string) : item ) as @pat: item)
         -> std/core/types/True;
      ((@skip main/Pair((@pat@2: name) : name, (@pat@3: value) : value, (@pat@4: (std/core/types/list :: V -> V)<annotation>) : (std/core/types/list :: V -> V)<annotation>, (@pat@5: std/core/types/string) : std/core/types/string) : item ) as @pat@1: item)
         -> std/core/types/False;
    };
  };
fun name/@copy // inline size: 2
  = fn(@this: name, name: (std/core/types/@optional :: V -> V)<std/core/types/string>){
    main/Name((match (name) {
      ((std/core/types/@Optional((@uniq-name@1408: std/core/types/string) : std/core/types/string) : (std/core/types/@optional :: V -> V)<std/core/types/string> ) as @pat: ((std/core/types/@optional :: V -> V)<std/core/types/string>))
         -> @uniq-name@1408;
      ((@skip std/core/types/@None() : (std/core/types/@optional :: V -> V)<std/core/types/string> ) as @pat@0: ((std/core/types/@optional :: V -> V)<std/core/types/string>))
         -> (match (@this) {
          ((@skip main/Name((@x: std/core/types/string) : std/core/types/string) : name ) as @pat@1: name)
             -> @x;
        });
    }));
  };
fun name/make // inline size: 3
  = fn(s: std/core/types/string){
    main/Name((match ((std/core/string/(==)(s, ""))) {
      ((std/core/types/True() : std/core/types/bool ) as @pat: std/core/types/bool)
         -> "xxx";
      ((@skip std/core/types/False() : std/core/types/bool ) as @pat@0: std/core/types/bool)
         -> s;
    }));
  };
inline borrow "^" fun value/string // inline size: 0
  = fn(value: value){
    match (value) {
      ((@skip main/Str((@x: std/core/types/string) : std/core/types/string) : value ) as @pat: value)
         -> @x;
    };
  };
fun value/@copy // inline size: 2
  = fn(@this: value, string: (std/core/types/@optional :: V -> V)<std/core/types/string>){
    main/Str((match (string) {
      ((std/core/types/@Optional((@uniq-string@1450: std/core/types/string) : std/core/types/string) : (std/core/types/@optional :: V -> V)<std/core/types/string> ) as @pat: ((std/core/types/@optional :: V -> V)<std/core/types/string>))
         -> @uniq-string@1450;
      ((@skip std/core/types/@None() : (std/core/types/@optional :: V -> V)<std/core/types/string> ) as @pat@0: ((std/core/types/@optional :: V -> V)<std/core/types/string>))
         -> (match (@this) {
          ((@skip main/Str((@x: std/core/types/string) : std/core/types/string) : value ) as @pat@1: value)
             -> @x;
        });
    }));
  };
inline fun annotation/@copy // inline size: 0
  = fn(@this: annotation){
    main/Export;
  };
inline fun annotation/show // inline size: 0
  = fn(an__derived_show: annotation){
    match (an__derived_show) {
      ((@skip main/Export() : annotation ) as @pat: annotation)
         -> "Export";
    };
  };
fun name/show // inline size: 4
  = fn(na__derived_show: name){
    std/core/types/(++)("Name(", (std/core/types/(++)("name=", (std/core/types/(++)((std/core/show/#string/show((match (na__derived_show) {
            ((@skip main/Name((@x: std/core/types/string) : std/core/types/string) : name ) as @pat: name)
               -> @x;
          }))), ")")))));
  };
fun value/show // inline size: 3
  = fn(va__derived_show: value){
    match (va__derived_show) {
      ((@skip main/Str((string: std/core/types/string) : std/core/types/string) : value ) as @pat: value)
         -> std/core/types/(++)("Str(string=", (std/core/types/(++)((std/core/show/#string/show(string)), ")")));
    };
  };
fun document/show // inline size: 4
  = fn(do__derived_show: document){
    std/core/types/(++)("Document(", (std/core/types/(++)("items=", (std/core/types/(++)((std/core/list/show<item,(std/core/types/total :: E)>((match (do__derived_show) {
              ((@skip main/Document((@x: (std/core/types/list :: V -> V)<item>) : (std/core/types/list :: V -> V)<item>) : document ) as @pat: document)
                 -> @x;
            }), main/#item/show)), ")")))));
  };
inline fun to-pattern // inline size: 2
  = fn(s: std/core/types/string){
    std/core/types/(++)("${", (std/core/types/(++)(s, "}")));
  };
fun main // inline size: 1
  = fn<<(std/core/types/div :: X)>>(){
    main/fixpoint<document,(std/core/types/total :: E)>(main/#example/document, main/#document/interp, main/#document/(==));
  };